# Sample flag dataset

Put one raster image per country in this folder.

Examples:
- `france.png`
- `germany.png`
- `japan.png`

The app derives the typed country name from the filename.

This directory is gitignored for image files so local datasets are not committed.
